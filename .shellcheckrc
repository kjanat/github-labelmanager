# ShellCheck configuration
# https://github.com/koalaman/shellcheck/wiki/Directive

# Use bash as default shell
shell=bash

# Exclude node_modules and other generated files
# Note: ShellCheck doesn't support exclude paths in config,
# use: shellcheck $(find . -name "*.sh" -not -path "*/node_modules/*")

# No global disables - use inline # shellcheck disable=SCXXXX comments
# for specific files that need exceptions
