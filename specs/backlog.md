# Carryover Backlog

This file preserves unresolved items from the removed legacy TODO plan.
These are intentionally tracked outside the v2 core execution graph.

## Items

1. Add and maintain `CHANGELOG.md`.
2. Add version bump automation for releases.
3. Optional: add dedicated integration tests against a mock GitHub API server.

## Ownership

- Product owner decides scheduling against v2 core milestones. `agent: @oracle`
- Items may be promoted into PRD tasks when they become in-scope.
